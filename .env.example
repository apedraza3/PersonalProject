# Environment Variables Template for Docker
# Copy this file to .env and fill in your actual values
# .env is in .gitignore and will NOT be committed

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# MySQL root password for Docker container
DB_PASSWORD=your_secure_db_password_here

# ===========================================
# JWT CONFIGURATION (REQUIRED)
# ===========================================
# Generate a secure random string (at least 256 bits)
# Example: openssl rand -base64 32
APP_JWT_SECRET=your_jwt_secret_key_at_least_256_bits_long

# ===========================================
# ENCRYPTION CONFIGURATION (REQUIRED)
# ===========================================
# AES-256 encryption key for encrypting Plaid access tokens
# MUST be 64 hexadecimal characters (32 bytes)
# Generate with: openssl rand -hex 32
APP_ENCRYPTION_KEY=your_64_character_hex_encryption_key_here

# ===========================================
# PLAID CONFIGURATION (REQUIRED)
# ===========================================
# Get your credentials from: https://dashboard.plaid.com/
PLAID_ENV=sandbox
PLAID_CLIENT_ID=your_plaid_client_id
PLAID_SECRET=your_plaid_secret
PLAID_PRODUCTS=transactions,balance,liabilities,investments
PLAID_COUNTRY_CODES=US
PLAID_REDIRECT_URI=http://localhost:8080/plaid/callback

# ===========================================
# SECURITY CONFIGURATION
# ===========================================
# Set to 'true' in production with HTTPS
COOKIES_SECURE=false

# ===========================================
# CORS CONFIGURATION
# ===========================================
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:3000

# ===========================================
# CRYPTOCURRENCY API KEYS (Optional)
# ===========================================
# Etherscan API for Ethereum blockchain data
# Get free key at: https://etherscan.io/myapikey
ETHERSCAN_API_KEY=

# CoinGecko API for crypto prices (free tier doesn't need key)
COINGECKO_API_KEY=

# ===========================================
# COINBASE OAUTH (Optional - for exchange integration)
# ===========================================
# Coinbase OAuth credentials for exchange integration
# Get credentials at: https://www.coinbase.com/settings/api
# Create an OAuth2 application and get your Client ID and Secret
COINBASE_CLIENT_ID=
COINBASE_CLIENT_SECRET=
COINBASE_REDIRECT_URI=http://localhost:8080/api/exchange/coinbase/callback

# ===========================================
# SPRING PROFILE
# ===========================================
# Use 'prod' for production environment
SPRING_PROFILES_ACTIVE=default
